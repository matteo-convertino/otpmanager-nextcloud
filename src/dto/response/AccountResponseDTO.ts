export type AccountResponseDTO = {
    id: number
    secret: string
    name: string
    issuer: string
    digits: number
    type: string
    period: number
    algorithm: number
    counter: number
    icon: string
    position: number
    userId: string
    createdAt: Date
    updatedAt: Date
    deletedAt: Date
}
